#include <stdio.h>
#include <stdlib.h>
#include "QUEUE.H"
int main()
{
    EeQueue(3);
    EeQueue(5);
    EeQueue(7);
    EeQueue(9);

if(Dequeue ()) {
    //1 True
    printf("Done!");
}
else {
    //0 False
        printf("Not Done! as the Queue is empty!");

}
    display();
    return 0;
}
