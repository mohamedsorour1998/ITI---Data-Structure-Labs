#include <stdio.h>
#include <stdlib.h>
#include "STACK IN LINKED LIST.h"
#include "QUEUE IN LINKED LIST.h"

int main()
{

Push(3);
Push(5);
Push(7);
Push(9);

Pop();

    display();/*

Stack stk ={.top = -1};

push(&stk, 10);
push(&stk, 20);
push(&stk, 30);

int num;

if (Pop(&stk, &num))
{
    //pop true
}
else {
        //pop false

}
queue que ={.front=-1, .rear=-1};
EnQueue (&que, 10);
EnQueue (&que, 20);
EnQueue (&que, 30);

if (DeQueue(&que, &num))
{
    //pop true
}
else {
        //pop false

}*/
    return 0;
}
